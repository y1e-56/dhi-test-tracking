PORT=5000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dhi_test_tracking
JWT_SECRET=change-me-in-production

# Resend (email notifications — utilisé sur Render et local, pas de blocage SMTP)
RESEND_API_KEY=

# SMTP (fallback local)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="DHI Test Tracking <onboarding@resend.dev>"
